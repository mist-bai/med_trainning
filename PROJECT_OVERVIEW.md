# 医药合规试题生成系统 — 技术特点与功能说明

## 一、技术特点

### 1. 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| **前端** | Next.js 14 + React 18 + TypeScript | App Router、服务端/客户端组件 |
| **前端样式** | Tailwind CSS | 响应式、深蓝/琥珀/青绿/紫等配色 |
| **后端** | FastAPI + Uvicorn | 异步 API、Pydantic 校验 |
| **工作流引擎** | LangGraph | 有状态图（检索 → 生成 → 人工审核） |
| **大模型** | LangChain-DeepSeek（DeepSeek Chat） | 合规题目生成、拒绝原因重生成 |
| **向量检索** | Qdrant + Sentence-Transformers | 384 维多语言向量，本地 MiniLM 模型 |
| **持久化** | MySQL + SQLAlchemy | 试题库、审核任务、自动迁移 |
| **缓存/中间态** | Redis | 待审核题目临时存储（24h 过期） |
| **部署** | Docker Compose | 前后端容器、med_bridge 网络 |
| **导出** | openpyxl | 审核通过试题导出 Excel |

### 2. 架构与流程特点

- **检索增强生成（RAG）**：用户输入关键词/主题 → 向量检索知识库（Qdrant）→ 将检索结果与用户要求一并交给 DeepSeek 生成题目。
- **人工在环（Human-in-the-Loop）**：生成题目后进入待审核；通过则落库，拒绝可填写原因并自动触发 DeepSeek 按原因重新生成。
- **多题型与批量**：支持单选题、多选题、判断题、主观题、填空题；支持一次生成 1～10 道题，每题独立审核任务。
- **前后端分离**：前端 Next.js 通过 rewrites 将 `/agent/*`、`/audit/*`、`/questions/*` 代理到后端，Docker 内使用服务名 `backend` 访问。

### 3. 数据与安全

- 配置与密钥通过 `.env` 管理，不提交仓库。
- 试题、审核任务、审批意见存 MySQL；待审核题目数据存 Redis，审核完成后可清理。
- 数据库支持自动迁移（如增加 `question_type`、`answer` 改为 TEXT）。

---

## 二、功能列表

### 1. 审核管理（`/admin/audit`）

| 功能 | 说明 |
|------|------|
| **按主题/关键词出题** | 顶部大输入框填写「题目主题或合规条款关键词」（如招待标准、学术会议禁令），作为检索与生成依据。 |
| **生成数量与题型** | 可选 1～10 道；题型：单选、多选、判断、主观、填空。 |
| **开始生成试题** | 调用智能体：检索知识库 → 生成题目 → 进入待审核列表；按钮有 Loading、禁用态。 |
| **待审核列表** | 卡片展示题目、选项/参考答案、解析；支持单选/多选/判断/主观/填空多种展示。 |
| **单题通过/拒绝** | 每题可填「拒绝原因」；通过则题目入库，拒绝则删除临时数据并可自动按原因重新生成。 |
| **全选与一键审批通过** | 可勾选部分或全部待审核题目，一键批量通过；未勾选时点击即全部通过。 |
| **审核通过试题库入口** | 顶部链接跳转到「审核通过试题库」页。 |
| **已向量化文档列表** | 可展开「已向量化文档（知识库）」区域，查看当前 Qdrant 知识库中的文档列表、每个文档的主题摘要（首段截取）及分块数量；底部提示后续将增加上传页面，配合 med_upload 工具扫描路径上传。 |

### 2. 审核通过试题库（`/admin/questions`）

| 功能 | 说明 |
|------|------|
| **列表展示** | 表格展示试题 ID、试题类型、题目、正确答案、审批意见、审核时间。 |
| **按时间筛选** | 全部 / 今天 / 近 7 天 / 近 30 天（按审核时间）。 |
| **按题型筛选** | 全部题型或指定：单选、多选、判断、主观、填空。 |
| **按主题/关键词搜索** | 输入关键词，对题目内容模糊匹配（不区分大小写）。 |
| **多选与导出 Excel** | 勾选题目后导出为 Excel（含试题类型、题目、选项、答案、解析、审批意见、审核时间）。 |
| **查看详情** | 弹层展示题目、选项/参考答案、解析、审批意见与审核时间。 |

### 3. 智能体与后端能力

| 能力 | 说明 |
|------|------|
| **按用户输入检索** | 使用 `question_context`（用户输入）做向量检索，从 Qdrant 取相关合规条款。 |
| **按题型与数量生成** | 根据题型生成对应格式（单选/多选/判断/主观/填空），支持一次多道。 |
| **拒绝原因重生成** | 拒绝时可将原因传给 DeepSeek，在新一轮生成中参考该原因改进题目。 |
| **多题多任务** | 一次生成多道题时，为每道题创建独立审核任务与 Redis 缓存，逐个审核、通过后逐个入库。 |

### 4. 数据模型（概要）

- **ExamQuestion**：试题 ID、题目、选项（JSON）、答案、解析、来源文档、试题类型、创建时间。
- **AuditTask**：任务 ID、关联试题 ID、状态（待审核/通过/拒绝）、审批意见、处理时间、创建时间。
- 审核通过后，AuditTask 与 ExamQuestion 关联，试题类型与长答案（主观/填空）均支持存储与导出。

---

## 三、API 一览（供对接/联调）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/test_connections` | 检测 Redis / MySQL / Qdrant 连通性 |
| POST | `/api/generate-question` | 生成试题（含数量、题型） |
| POST | `/agent/start` | 触发智能体生成（query、question_count、question_type） |
| GET | `/audit/list` | 待审核题目列表 |
| POST | `/audit/action/{task_id}` | 提交审核（通过/拒绝 + 意见） |
| GET | `/questions/approved` | 审核通过试题列表 |
| GET | `/questions/export?ids=1,2,3` | 导出指定 ID 的试题为 Excel |
| GET | `/documents/vectorized` | 已向量化文档列表（含主题摘要、分块数） |
| GET | `/api/audit-tasks` | 审核任务列表（可按状态筛选） |
| GET | `/api/questions` | 试题分页列表 |

---

## 四、后续规划（上传与 med_upload）

- **上传页面**：计划增加独立「上传文件」页面，支持选择/输入本地目录或文件路径，一键触发上传并向量化。
- **med_upload 工具**：本地已有专用工具 `med_upload`，用于扫描文件路径、解析 PDF 等文档并写入 Qdrant 知识库；上传页面将调用或集成该工具，实现「选择路径 → 自动扫描 → 上传并向量化」的流程。

---

## 五、小结

本系统是一套**医药合规场景下的 RAG + 人工审核**试题生成与管理平台：用户输入主题或关键词，系统从向量知识库检索条款，用 DeepSeek 生成单选/多选/判断/主观/填空题，经人工审核（含全选一键通过）后入库，并支持按时间、题型、主题筛选、导出 Excel 与查看审批意见，适合内部培训与合规考核使用。
