# 防火墙配置说明

## 当前状态

✅ **iptables 规则已添加**
- 3000 端口已在 iptables INPUT 链的第一条规则中允许
- 规则位置：在 REJECT 规则之前，确保生效

## 问题排查

### 1. 服务器端检查（已完成）

- ✅ 端口监听：`0.0.0.0:3000` 正在监听
- ✅ Docker 端口映射：`0.0.0.0:3000->3000/tcp`
- ✅ Next.js 绑定：`Network: http://0.0.0.0:3000`
- ✅ iptables 规则：已添加 `ACCEPT tcp dpt:3000`

### 2. 云服务器安全组配置（需要检查）

**如果使用阿里云/腾讯云等云服务器，需要在安全组中开放 3000 端口：**

#### 阿里云安全组配置步骤：

1. 登录阿里云控制台
2. 进入 **ECS 实例** → 选择你的服务器
3. 点击 **安全组** → **配置规则**
4. 点击 **添加安全组规则**
5. 配置如下：
   - **规则方向**：入方向
   - **授权策略**：允许
   - **协议类型**：自定义 TCP
   - **端口范围**：3000/3000
   - **授权对象**：0.0.0.0/0（或指定你的 IP）
   - **描述**：允许访问前端服务
6. 点击 **保存**

#### 腾讯云安全组配置步骤：

1. 登录腾讯云控制台
2. 进入 **云服务器 CVM** → 选择你的服务器
3. 点击 **安全组** → **修改规则**
4. 点击 **添加规则**
5. 配置同上

### 3. 测试访问

配置完成后，在浏览器或使用 curl 测试：

```bash
# 从外部测试
curl -v http://8.140.248.43:3000

# 或直接在浏览器访问
http://8.140.248.43:3000
```

### 4. 持久化 iptables 规则

当前规则已添加，但重启后可能会丢失。如果需要持久化：

```bash
# CentOS/RHEL 7
iptables-save > /etc/sysconfig/iptables
systemctl enable iptables

# Ubuntu/Debian
iptables-save > /etc/iptables/rules.v4
```

## 当前配置总结

- **服务器 IP**：8.140.248.43
- **前端端口**：3000
- **后端端口**：8000
- **iptables 规则**：已添加并生效
- **Docker 映射**：正常
- **Next.js 绑定**：0.0.0.0:3000

**最可能的问题**：云服务器安全组未开放 3000 端口，需要在云控制台配置。
